# Doubao-音频生成-1.0 体验报告发布版

## 使用方式

直接打开 `index.html` 即可查看完整报告。

目录结构：

- `index.html`：发布版 H5 报告。
- `audio/`：报告中用到的全部音频，已统一压缩为 MP3。

## 注意事项

该目录是发布版副本，不影响上一级目录的原始报告和原始 WAV 音频。

如果要部署到公网静态服务，请保持 `index.html` 和 `audio/` 目录在同一级。

